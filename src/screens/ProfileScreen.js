
import React, { Component } from 'react';
import { View, Text } from 'react-native';
import { ProfileStyles } from "../utils/styles";
const ProfileScreen = () => {
    return (
        <View style={ProfileStyles.container}>
            <Text>ProfileScreen</Text>
        </View>
    );
};

export default ProfileScreen;
